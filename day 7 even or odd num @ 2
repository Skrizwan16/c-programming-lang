#include <stdio.h>
int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);

    if(num % 2 == 0)
        printf("%d given number is even.", num);
    else
        printf("%d given number is odd.", num);
    
    return 0;
}
